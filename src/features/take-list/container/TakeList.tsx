import React from 'react';
import {Text} from 'react-native';
import takeListStyle from 'features/take-list/style/TakeListStyle';

const TakeList: React.FC = () => {
  return <Text style={takeListStyle.container}>¡Hola tomar lista!</Text>;
};

export default TakeList;
