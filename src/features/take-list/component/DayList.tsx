import React from 'react';
import {Text} from 'react-native';
import DayListStyle from 'features/take-list/style/DayListStyle';

const DayList: React.FC = () => {
  return <Text style={DayListStyle.container}>¡Hola lista de días!</Text>;
};

export default DayList;
